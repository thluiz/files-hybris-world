node_modules/
dist/
.astro/
.wrangler/

# Local secrets (IP_SALT)
.dev.vars

# Access codes NEVER go into git — this repository is public.
db/seed-codes.sql
db/codes.csv
db/labels.txt
# Backup copies of these lists, made before touching the levels.
db/*.bak

# PDFs live in R2, not here.
*.pdf
